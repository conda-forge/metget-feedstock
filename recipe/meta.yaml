{% set name = "metget" %}
{% set version = "0.14.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/waterinstitute/MetGet/archive/refs/tags/{{ version }}.tar.gz
  sha256: 95d3c758355ebfe4b091775a78261914a4c2657202e2ea93a55cc51d16a2bd35

build:
  entry_points:
    - metget = metget:metget_client_cli
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=61.0.0
    - wheel
    - pip
  run:
    - python >={{ python_min }}
    - requests
    - yaspin
    - prettytable

test:
  imports:
    - metget
  commands:
    - pip check
    - metget --help
    - metget --version
  requires:
    - pip
    - python {{ python_min }}

about:
  summary: A client package for interaction with a MetGet server instance
  home: https://github.com/waterinstitute/metget
  dev_url: https://github.com/waterinstitute/metget
  license: MIT
  license_family: MIT
  license_file: LICENSE.md

extra:
  recipe-maintainers:
    - zcobell
